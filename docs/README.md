# uranometria documentation

- [Sky charts](charts.md): the `chart` command, the interactive HTML page it
  produces, and how to regenerate your map as your library grows.
- [Chart config reference](chart-config.md): every key the YAML accepts, the
  three ways to specify an object, and which designations resolve offline.
- [Annotating images](annotate.md): plate-solving with ASTAP, catalog
  cross-matching, and rendering the annotated PNG.
- [The annotation model](annotation-model.md): the JSON contract field by
  field, and the generate/edit/re-render workflow.

For install instructions and a quick tour, start with the
[top-level README](../README.md).
